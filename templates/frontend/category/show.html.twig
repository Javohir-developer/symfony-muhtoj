{% extends 'base.html.twig' %}

{% block body %}
    <div class=" container example-wrapper">
        <h1>{{ category.name }}</h1>
        <a href="{{ path('front_category_edit', {'id': category.id}) }}" class="btn btn-info" role="button">edit</a>
        <a href="{{ path('front_category_delete', {'id': category.id}) }}" class="btn btn-danger" role="button">delete</a>
    </div>
{% endblock %}
